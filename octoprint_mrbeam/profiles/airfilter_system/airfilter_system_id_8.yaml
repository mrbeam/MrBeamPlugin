#airfilter 3 system
prefilter_stages: 2
carbonfilter_stages: 1
prefilter:
  - lifespan: 80 # TODO replace with correct value SW-2320
    shopify_link: 'maintenance/af3/pf2' # no utm parameters needed, as the redirect already use these
  - lifespan: 100 # TODO replace with correct value SW-2320
    shopify_link: 'maintenance/af3/pf2' # no utm parameters needed, as the redirect already use these
carbonfilter:
  - lifespan: 400 # TODO replace with correct value SW-2320
    shopify_link: 'maintenance/af3/mf' # no utm parameters needed, as the redirect already use these
